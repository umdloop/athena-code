<?xml version="1.0" encoding="UTF-8"?>
<robot xmlns:xacro="http://wiki.ros.org/xacro" name="athena_drive">

  <!-- Use this if parameters are set from the launch file, otherwise delete -->
  <xacro:arg name="prefix" default=""/>
  <xacro:arg name="use_mock_hardware" default="false"/>
  <xacro:arg name="mock_sensor_commands" default="false"/>
  <xacro:arg name="sim_gazebo" default="true"/>
  <xacro:arg name="simulation_controllers" default=""/>
  <xacro:arg name="deactivate_odrive" default="false"/>
  <xacro:arg name="steering_config" default="rear"/>
  <xacro:arg name="can_interface" default="can0"/>


  <xacro:include filename="$(find description)/urdf/drive/athena_drive_macro.xacro"/>
  <xacro:include filename="$(find description)/urdf/drive/athena_drive_macro.ros2_control.xacro"/>
  <xacro:include filename="$(find description)/urdf/components/imu.xacro"/>
  <xacro:include filename="$(find description)/urdf/components/gps.xacro"/>
  <xacro:include filename="$(find description)/urdf/components/magnetometer.xacro"/>
  <xacro:include filename="$(find description)/urdf/components/ground_truth_odom.xacro"/>
  <xacro:include filename="$(find description)/urdf/components/zed_camera.xacro"/>


  <!-- Load robot's macro with parameters -->
  <!-- set prefix if multiple robots are used -->
  <!--
  <xacro:athena_drive prefix="$(arg prefix)" parent="world">
    <origin xyz="0 0 0" rpy="0 0 0"/>
  </xacro:athena_drive>
  -->

  
  <!-- Import athena_drive macro -->
  <xacro:include filename="$(find description)/urdf/drive/athena_drive_description.urdf.xacro"/>

  <!-- Import rmd ros2_control description -->
  <xacro:include filename="$(find description)/ros2_control/drive/drive.rmd.ros2_control.xacro" />

  <!-- Import odrive ros2_control description -->
  <xacro:include filename="$(find description)/ros2_control/drive/drive.odrive.ros2_control.xacro" />

  <!-- Import LED ros2_control description -->
  <xacro:include filename="$(find description)/ros2_control/drive/drive.led.ros2_control.xacro" />

  <!-- Import power_module ros2_control description -->
  <xacro:include filename="$(find description)/ros2_control/drive/drive.power_module.ros2_control.xacro" />

  <!-- Contains description of drive -->
  <xacro:description/>


  <xacro:imu_sensor parent="base_footprint">
    <origin xyz="0.0 0.0 0.0" rpy="0 0 0"/>
  </xacro:imu_sensor>

  <xacro:gps_sensor parent="base_footprint">
    <origin xyz="0.0 0.0 0.0" rpy="0 0 0"/>
  </xacro:gps_sensor>

  <xacro:magnetometer_sensor parent="base_footprint">
    <origin xyz="0.0 0.0 0.0" rpy="0 0 0"/>
  </xacro:magnetometer_sensor>

  <xacro:ground_truth_odometry robot_base_frame="base_footprint"/>
 
  <xacro:zed_camera_sensor parent="base_footprint" camera_name="zed" sim_gazebo="$(arg sim_gazebo)">
    <origin xyz=".3 -0.123 0.073" rpy="0 0 0"/>
  </xacro:zed_camera_sensor>

  <xacro:if value="$(arg use_mock_hardware)">
    <xacro:athena_drive_ros2_control
    name="athena_drive"
    prefix="$(arg prefix)"
    use_mock_hardware="$(arg use_mock_hardware)"
    mock_sensor_commands="$(arg mock_sensor_commands)"
    sim_gazebo="$(arg sim_gazebo)"
    simulation_controllers="$(arg simulation_controllers)"/>
  </xacro:if>
  <xacro:unless value="$(arg use_mock_hardware)">

    <!-- ODrives-->
    <xacro:odrive_ros2_control
      name="odrive"
      deactivate_odrive="$(arg deactivate_odrive)"
      steering_config="$(arg steering_config)"
      can_interface="$(arg can_interface)"/>

    <!-- RMD -->
    <xacro:rmd_ros2_control name="rmd" can_interface="$(arg can_interface)"/>

    <!-- LED status indicator (GPIO-based) -->
    <xacro:led_ros2_control name="drive_led" can_interface="$(arg can_interface)"/>

    <!-- PowerModule (CAN-based) -->
    <xacro:power_module_ros2_control name="drive_power_module" can_interface="$(arg can_interface)"/>
  </xacro:unless>

  
</robot>
